# AmbedkarGPT-Intern-Task
A local RAG pipeline that loads a speech text file, chunks it, creates embeddings with HuggingFace, stores them in ChromaDB, retrieves relevant parts for any query, and generates answers using Ollama’s Mistral 7B model through LangChain. Fully offline, free, and Python-based.
